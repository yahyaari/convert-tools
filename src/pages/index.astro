---
import BaseLayout from "../layouts/BaseLayout.astro";
import SITE from "../consts";

const tools = [
    {
        key: "img",
        href: "/image-converter#top",
        title: "Saku Görsel Dönüştürücü (PNG / JPG / WebP)",
        desc: "Saku dönüştürücü ile PNG, JPG ve WebP formatları arasında hızlı dönüşüm yapın.",
        cta: "Hemen Dönüştür →",
        color: "blue",
        icon: `
      <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
        fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <rect width="18" height="18" x="3" y="3" rx="2" ry="2"></rect>
        <circle cx="9" cy="9" r="2"></circle>
        <path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"></path>
      </svg>
    `,
    },
    {
        key: "bg",
        href: "/background-remover#top",
        title: "Saku Arka Plan Silici (Background Remover)",
        desc: "Saku arka plan silici ile görsellerin arka planını tek tıkla kaldırın (tarayıcı içinde).",
        cta: "Arka Planı Sil →",
        color: "purple",
        icon: `
      <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"
        fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M4 22V4c0-.5.2-1 .6-1.4C5 2.2 5.5 2 6 2h12c.5 0 1 .2 1.4.6.4.4.6.9.6 1.4v18l-4-4-4 4-4-4-4 4Z"></path>
      </svg>
    `,
    },
];

const comingSoon = [
    { title: "PDF Compressor", desc: "PDF boyutunu küçült (kaliteyi koru)." },
    { title: "DOCX ↔ PDF", desc: "Word ↔ PDF dönüştürme (yakında)." },
    { title: "Batch Convert", desc: "Toplu görsel dönüştürme." },
];

const pageUrl = new URL("/", SITE.url).toString();

// (SEO) WebPage’ye mainEntity olarak iki aracı da yazalım (rich result için iyi)
const mainEntityApps = [
    {
        "@type": "SoftwareApplication",
        name: "Saku Image Converter",
        alternateName: [
            "Saku PNG Dönüştürücü",
            "Saku Görsel Dönüştürücü",
            "Saku PNG Converter",
        ],
        applicationCategory: "MultimediaApplication",
        operatingSystem: "Web",
        url: new URL("/image-converter", SITE.url).toString(),
        isAccessibleForFree: true,
        offers: { "@type": "Offer", price: "0", priceCurrency: "TRY" },
        publisher: { "@id": `${SITE.url}/#organization` },
        inLanguage: ["tr", "en"],
        description:
            "Saku Convert ile PNG, JPG ve WebP formatları arasında hızlı dönüşüm yapın. Ücretsiz görsel dönüştürücü; işlemler tarayıcı içinde, dosyalar yüklenmez.",
    },
    {
        "@type": "SoftwareApplication",
        name: "Saku Background Remover",
        alternateName: ["Saku Arka Plan Silici", "Saku Background Remover"],
        applicationCategory: "MultimediaApplication",
        operatingSystem: "Web",
        url: new URL("/background-remover", SITE.url).toString(),
        isAccessibleForFree: true,
        offers: { "@type": "Offer", price: "0", priceCurrency: "TRY" },
        publisher: { "@id": `${SITE.url}/#organization` },
        inLanguage: ["tr", "en"],
        description:
            "Saku arka plan silici ile görsellerin arka planını tek tıkla kaldırın. Tamamen tarayıcı içinde çalışır, dosyalar sunucuya yüklenmez.",
    },
];
---

<BaseLayout
    title="Saku Convert – PNG, JPG, WebP Görsel Dönüştürücü + Arka Plan Silici"
    description="Saku Convert: Ücretsiz PNG/JPG/WebP dönüştürücü ve arka plan silici. Tamamen tarayıcı içinde çalışır, upload yok."
    canonical="/"
    lang="tr"
    ogImage="/og/og-home.png"
    jsonLd={[
        {
            "@type": "WebPage",
            "@id": `${pageUrl}#webpage`,
            url: pageUrl,
            name: "Saku Convert – PNG/JPG/WebP Görsel Dönüştürücü ve Arka Plan Silici",
            inLanguage: "tr",
            isPartOf: { "@id": `${SITE.url}/#website` },
            about: { "@id": `${SITE.url}/#organization` },
            description:
                "Saku Convert: Ücretsiz PNG/JPG/WebP görsel dönüştürücü ve arka plan silici. Tamamen tarayıcı içinde çalışır, dosyalar sunucuya yüklenmez.",
            primaryImageOfPage: {
                "@type": "ImageObject",
                url: new URL("/og/og-home.png", SITE.url).toString(),
                width: 1200,
                height: 630,
            },
            mainEntity: mainEntityApps,
        },
        {
            "@type": "BreadcrumbList",
            "@id": `${pageUrl}#breadcrumb`,
            itemListElement: [
                {
                    "@type": "ListItem",
                    position: 1,
                    name: "Anasayfa",
                    item: pageUrl,
                },
            ],
        },
    ]}
>
    <header class="topbar">
        <div class="container topbar-inner">
            <nav class="nav nav-left" aria-label="Primary left">
                <a href="#roadmap" id="t-navRoadmap">Roadmap</a>
            </nav>

            <a class="brand" href="/" aria-label="Convert Tools">
                <span class="brand-mark">CT</span>
            </a>

            <div class="topbar-right">
                <nav class="nav nav-right" aria-label="Primary right">
                    <a href="#tools" id="t-navTools">Araçlar</a>
                </nav>

                <div class="lang-switch" aria-label="Language switch">
                    <button class="lang-btn" type="button" data-lang="tr">
                        TR
                    </button>
                    <button class="lang-btn" type="button" data-lang="en">
                        EN
                    </button>
                </div>
            </div>
        </div>
    </header>

    <section class="hero-wrap" id="top">
        <div class="container">
            <div class="hero-box">
                <span class="badge" id="t-badge">Ücretsiz & Sınırsız</span>
                <h1 id="t-heroTitle">
                    Saku Convert – Ücretsiz Görsel Dönüştürücü & Background
                    Remover
                </h1>
                <p id="t-heroDesc">
                    Saku Convert ile PNG, JPG ve WebP görsellerinizi ücretsiz
                    şekilde dönüştürün. Saku görsel dönüştürücü tamamen tarayıcı
                    içinde çalışır, dosyalarınız sunucuya yüklenmez.
                </p>

                <div class="hero-cta">
                    <a class="primary-btn" href="#tools" id="t-cta">
                        Araçları Gör →
                    </a>
                    <span class="hero-note muted" id="t-note">
                        🔒 Dosyalar sunucuya yüklenmez
                    </span>
                </div>
            </div>
        </div>
    </section>

    <section class="tools-grid" id="tools">
        <div class="container">
            <div class="section-head">
                <h2 id="t-toolsHead">Araçlar</h2>
                <p class="muted" id="t-toolsDesc">
                    Hızlı ve güvenli dijital medya araçlarımızla tanışın:
                </p>
            </div>

            <div class="grid">
                {
                    tools.map((t) => (
                        <a
                            href={t.href}
                            class="tool-card"
                            title={`Saku Convert - ${t.title}`}
                        >
                            <div
                                class={`card-icon ${t.color}`}
                                set:html={t.icon}
                            />
                            <h3 id={`t-${t.key}-title`}>{t.title}</h3>
                            <p id={`t-${t.key}-desc`}>{t.desc}</p>
                            <small class="muted" id={`t-${t.key}-hint`}>
                                {t.key === "img"
                                    ? "PNG / JPG / WebP dönüştürücü"
                                    : "Arka plan silici (PNG şeffaf)"}
                            </small>
                            <span class="btn-text" id={`t-${t.key}-cta`}>
                                {t.cta}
                            </span>
                        </a>
                    ))
                }
            </div>
        </div>
    </section>

    <section class="features-wrap">
        <div class="container">
            <div class="features-box">
                <div class="features-inner">
                    <span id="t-f1">⚡ Hızlı İşlem</span>
                    <span id="t-f2">
                        🔒 %100 Güvenli (Dosyalar sunucuya yüklenmez)
                    </span>
                    <span id="t-f3">✨ Yüksek Kalite</span>
                </div>
            </div>
        </div>
    </section>

    <section class="roadmap-wrap" id="roadmap">
        <div class="container">
            <div class="roadmap-box">
                <div class="section-head">
                    <h2 id="t-roadmapHead">Yakında</h2>
                    <p class="muted" id="t-roadmapDesc">
                        Sürekli yeni araçlar ekliyoruz. İşte üzerinde
                        çalıştığımız bazı özellikler:
                    </p>
                </div>

                <div class="soon-grid">
                    {
                        comingSoon.map((c) => (
                            <div class="soon-card">
                                <div class="soon-title">{c.title}</div>
                                <div class="soon-desc">{c.desc}</div>
                            </div>
                        ))
                    }
                </div>
            </div>
        </div>
    </section>

    <section class="seo-text">
        <div class="container">
            <h2>Saku Dönüştürücü Nedir?</h2>
            <p>
                Saku Convert, PNG, JPG ve WebP formatları arasında hızlı dönüşüm
                yapmanızı sağlayan ücretsiz bir görsel dönüştürücüdür. Saku
                dönüştürücü araçları tamamen tarayıcı içinde çalışır ve
                dosyalarınız sunucuya yüklenmez.
            </p>

            <p>
                “Saku PNG dönüştürücü” arayan kullanıcılar için geliştirilen bu
                araç sayesinde görsellerinizi saniyeler içinde güvenle
                dönüştürebilirsiniz.
            </p>
            <p>
                Ücretsiz dönüştürücü arayanlar için Saku Convert; PNG
                dönüştürücü, JPG dönüştürücü ve WebP dönüştürücü ihtiyaçlarını
                tek sayfada çözer. Görsel dönüştürme işlemi hızlıdır ve tüm
                dönüşümler tarayıcınızda gerçekleşir.
            </p>
        </div>
    </section>

    <footer class="footer-wrap">
        <div class="container">
            <div class="footer-box footer-inner">
                <span
                    >© {new Date().getFullYear()} · A project by Saku Studios</span
                >
                <span class="muted" id="t-footerRight">
                    All processing happens in your browser.
                </span>
            </div>
        </div>
    </footer>

    <script type="module">
        import { i18n } from "/i18n.js";
        import { detectLang } from "/utils/lang.js";

        const lang = detectLang();
        const t = i18n[lang] || i18n.en;

        const set = (id, value) => {
            const el = document.getElementById(id);
            if (el && value != null) el.textContent = value;
        };

        // Landing text
        set("t-badge", t.badge);
        set("t-heroTitle", t.heroTitle);
        set("t-heroDesc", t.heroDesc);
        set("t-cta", t.cta);
        set("t-note", t.note);

        set("t-toolsHead", t.toolsHead);
        set("t-toolsDesc", t.toolsDesc);

        set("t-roadmapHead", t.roadmapHead);
        set("t-roadmapDesc", t.roadmapDesc);

        set("t-footerRight", t.footerRight);

        // Nav labels
        set("t-navTools", t.toolsHead);
        set("t-navRoadmap", "Roadmap");

        // Features line
        if (lang === "en") {
            set("t-f1", "⚡ Fast");
            set("t-f2", "🔒 100% Secure (no uploads)");
            set("t-f3", "✨ High Quality");
        }

        // Tool cards
        if (lang === "tr") {
            set("t-img-title", "Saku Görsel Dönüştürücü (PNG/JPG/WebP)");
            set(
                "t-img-desc",
                "Saku PNG dönüştürücü ile PNG, JPG ve WebP formatları arasında hızlı dönüşüm yapın.",
            );
            set("t-img-cta", "Hemen Dönüştür →");

            set("t-bg-title", "Saku Arka Plan Silici");
            set(
                "t-bg-desc",
                "Saku background remover ile arka planı tek tıkla kaldırın (tarayıcı içinde).",
            );
            set("t-bg-cta", "Arka Planı Sil →");
        } else {
            set("t-img-title", "Saku Image Converter (PNG/JPG/WebP)");
            set(
                "t-img-desc",
                "Convert PNG, JPG, and WebP instantly with Saku Convert — free and in-browser.",
            );

            set("t-img-hint", "PNG / JPG / WebP converter");
            set("t-img-cta", "Convert Now →");

            set("t-bg-title", "Saku Background Remover");
            set(
                "t-bg-desc",
                "Remove image backgrounds in one click with Saku — fully in-browser.",
            );

            set("t-bg-hint", "Background remover (transparent PNG)");
            set("t-bg-cta", "Remove Background →");
        }

        document.documentElement.lang = lang;

        // ===== Language switch =====
        const langBtns = document.querySelectorAll(".lang-btn");

        function applyLangActive() {
            const current = localStorage.getItem("lang") || lang;
            langBtns.forEach((b) =>
                b.classList.toggle("is-active", b.dataset.lang === current),
            );
        }

        langBtns.forEach((btn) => {
            btn.addEventListener("click", () => {
                const next = btn.dataset.lang;
                localStorage.setItem("lang", next);
                location.reload();
            });
        });

        applyLangActive();
    </script>
</BaseLayout>
